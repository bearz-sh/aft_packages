name: vault
description: |
  ecure, store, and tightly control access to tokens,
  passwords, certificates, and encryption keys for protecting
  secrets and other sensitive data using a UI, CLI, or HTTP API.
service: watchtower
version: "0.0.1"
site: https://www.vaultproject.io/
image: 
  name: containrrr/watchtower
  tag: latest